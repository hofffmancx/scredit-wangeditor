<!-- 本页面对应的CSS样式表：products.scss -->

<div class="container" style="margin-top:30px;">
  <!-- <div class="row" style="background-color:#e9eaec;"> -->
<div class="row">
    <% if @product.category == "课程" %>
      <%= render "course_show" %>
    <% else %>
      <%= render "product_show" %>

    <% end %>

  </div>
</div>
